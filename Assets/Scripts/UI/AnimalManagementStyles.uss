/* Main panel background */
.main-panel-background {
    background-image: resource('UI/background');
    -unity-background-image-tint-color: rgb(255, 255, 255);
    -unity-background-scale-mode: stretch-to-fill;
}

/* Selection panel background */
.selection-panel-background {
    background-image: resource('UI/background');
    -unity-background-image-tint-color: rgb(255, 255, 255);
    -unity-background-scale-mode: stretch-to-fill;
}

/* Green button - normal state */
.green-button {
    background-image: resource('UI/button');
    -unity-background-scale-mode: stretch-to-fill;
    border-width: 0;
    color: rgb(255, 255, 255);
    -unity-font-style: bold;
}

/* Green button - hover state */
.green-button:hover {
    -unity-background-image-tint-color: rgb(220, 220, 220);
}

/* Green button - pressed state */
.green-button:active {
    background-image: resource('UI/button_pressed');
}

/* Red button - normal state */
.red-button {
    background-image: resource('UI/redbutton');
    -unity-background-scale-mode: stretch-to-fill;
    border-width: 0;
    color: rgb(255, 255, 255);
    -unity-font-style: bold;
}

/* Red button - hover state */
.red-button:hover {
    -unity-background-image-tint-color: rgb(220, 220, 220);
}

/* Red button - pressed state */
.red-button:active {
    background-image: resource('UI/redbutton_pressed');
}

/* Tab button - normal state */
.tab-button {
    background-image: resource('UI/button');
    -unity-background-scale-mode: stretch-to-fill;
    border-width: 0;
    color: rgb(255, 255, 255);
    padding: 8px 16px;
    margin-left: 5px;
    margin-right: 5px;
}

/* Tab button - hover state */
.tab-button:hover {
    -unity-background-image-tint-color: rgb(220, 220, 220);
}

/* Tab button - active/selected state */
.tab-button-active {
    background-image: resource('UI/button_pressed');
}

/* Exit/Close button - uses round exit button sprite */
.exit-button {
    background-image: resource('UI/exitbutton');
    -unity-background-scale-mode: stretch-to-fill;
    border-width: 0;
    width: 40px;
    height: 40px;
    padding: 0;
}

.exit-button:hover {
    -unity-background-image-tint-color: rgb(220, 220, 220);
}

/* Animal slot - hover effect */
.animal-slot-hover:hover {
    background-color: rgba(70, 70, 70, 0.9);
}

/* Animal slot - selected state */
.animal-slot-selected {
    background-color: rgba(80, 120, 80, 0.9);
    border-color: rgb(150, 200, 150);
    border-width: 2px;
}

/* Selection item - default state */
.selection-item {
    padding: 8px;
    margin-bottom: 3px;
    background-color: rgba(40, 40, 40, 0.9);
    border-radius: 3px;
}

/* Selection item - hover state */
.selection-item:hover {
    background-color: rgba(60, 100, 60, 0.9);
}

/* Info label styling */
.info-label {
    color: rgb(255, 255, 255);
    font-size: 14px;
    margin-bottom: 5px;
}

/* Info value styling */
.info-value {
    color: rgb(200, 200, 200);
    font-size: 12px;
    margin-left: 10px;
}
